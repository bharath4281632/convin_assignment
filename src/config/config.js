import loginForm from "./loginFormConfig";
import registrationForm from "./registerFormConfig";
import restApi from "./restApiConfig";
const config = {
  restApi,
  registrationForm,
  loginForm
};

export default config;
